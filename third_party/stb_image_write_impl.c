/* stb_image_write — l'unique unité de traduction qui porte l'implémentation.
 * Compilée à part (warnings stb tolérés ici, jamais dans le code SCPS). */
#define STB_IMAGE_WRITE_IMPLEMENTATION
#include "stb_image_write.h"
