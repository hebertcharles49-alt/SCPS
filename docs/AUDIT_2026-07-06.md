# AUDIT COMPLET — 2026-07-06 (synthèse 2026-07-07)

**Cible** : SCPS, SAVE_VERSION 72 (moteur C99 déterministe + front Godot 4).
**Méthode** : 5 voies d'audit en lecture seule (verbes→UI · mécaniques mortes · restes/différés · membrane/façade/tunables · correctness/save/déterminisme), chacune suivie d'une **vérification adversariale indépendante** (re-grep du source + re-mesures sur les binaires chronicle prébâtis, seeds 7/9/11/42/99 × 150-250 ans). Les verdicts des vérificateurs PRIMENT sur les lanes brutes. La lane « mécaniques mortes » d'origine ayant rendu un stub, son vérificateur a refait la dimension entière lui-même (12 items, mesures chronicle).
**Fraîcheur** : trois corrections post-snapshot appliquées à la synthèse (travail atterri le 07) — voir la note en fin de rapport.

---

## RÉSUMÉ EXÉCUTIF

### La question prioritaire : couverture verbe→UI

**38/38 verbes joueur (`CMD_*`) sont câblés de bout en bout** : enum `scps_sim.h:62-105` → case de drain (`scps_sim.c:328-626`, aucun verbe orphelin) → façade `scps_player_*` (`scps_api.h`) → binding `ClassDB::bind_method` (`scps_sim_node.cpp`) → **appel GDScript derrière un bouton réel** (≥1 call-site hors probes pour chacun). Le trou signalé par l'agent CODEX antérieur (manumit/slave_buy/slave_sell/council_pay sans UI) est **FERMÉ** (câblage servile V3 + Conseil V2a). Seul résidu : une chaîne de codex périmée (`codex.gd:47` prétend encore que la paie du Conseil n'a « aucun curseur » alors qu'il existe et fonctionne, `sidebar_drawer.gd:294-308`).

### Les défauts RÉELS les plus importants (CONFIRMED, high → med)

| # | Sév. | Défaut | Évidence |
|---|------|--------|----------|
| 1 | **high** | **Cooldowns de révolte non sérialisés** (`g_revolt_grace`/`g_coup_grace`/`g_concede_cd`) : accumulateurs inter-ticks qui gatent révolte/coup/concession, RAZ seulement par `sim_init` → **save/reload ≠ continuation** (une révolte peut éclater juste après reload là où le fil continu restait silencieux). Classe EMOB(v57)/COLC(v61)/TXYR(v65). Invisible au `--savetest` (same-process). | scps_revolt.c:118/124/206, :290/:344/:913, :406/:867/:930 ; scps_save.c (RVLT = `*s->rs` seul) |
| 2 | **high** | **Bouton Construction NO-OP** : le panneau ne grise que par la TECH (`debloque`) ; le drain refuse SILENCIEUSEMENT sur or/matière (`agency_build_acct` → return false, bool jeté) mais `player_build` retourne 1 → flash « ⚒ ordre émis » mensonger, zéro édifice. Le reader d'affordabilité `scps_build_legal` existe mais n'est **ni bindé ni consommé** (et ne teste pas la matière). | construction_panel.gd:86/167-169 ; scps_sim.c:336 ; scps_agency.c:336/345 ; scps_api.c:1221 |
| 3 | **med** | **FIN_SANG invisible à la membrane readout** — aggravé par le vérificateur : `endgame_readout` rabat FIN_SANG sur RFIN_AUCUNE (`fin`=0) → le bandeau ne nomme pas la 5e fin ET **l'écran d'épilogue ne s'ouvre JAMAIS** sur une fin SANG (`main.gd:343-347` latche sur `fin>0`). Seul le lavis carte (via `fin_raw`) fonctionne. | scps_readout.c:473-478 ; scps_readout.h:285 ; scps_api.c:598/604 ; endgame_banner.gd:51 ; main.gd:343-347 |
| 4 | **med** | **Dead-write d'armes IA** : `scps_ai.c:1307` écrit `+20 RES_ARMS` dans la vue transiente `region[]`, écrasée au prochain `econ_aggregate_regions` — l'or est réellement débité (`credit_spend`), les armes s'évaporent. Doublement mort : la levée consomme les macros `RES_ARMS_LIGHT/HEAVY/…`, pas la base `RES_ARMS`. | scps_ai.c:1303/1307 ; scps_econ.c:890/923/993/1813 ; scps_army.c:109-114 |
| 5 | **med** | **`g_hub_of`/`g_hub_dist` sérialisés sans revalidation** : `save_sane` ne borne pas la section ITRD ; `e->region[hub]` est déréférencé sans borne haute (int16_t ≤ 32767 vs SCPS_MAX_REG=832) → **lecture OOB sur save forgée**. Aggravant : `intertrade_buy_cost` (atteint au 1er tick de bâti post-load via agency) n'a pas de rebuild dirty. Le fuzztest ne forge jamais g_hub_of. | scps_intertrade.c:816-831, :436/:530 ; scps_save.c:182-345 ; viewer.c:243-251 |
| 6 | **med** | **6 `tune_f` hors registre** (BT_ATK_RATIO · BT_DECROCHE · BT_DEF_EDGE · BT_RELIEF_FALL · RELIG_MINORITY_SAT · SEED_PROV_CAP_MULT) : marchent au défaut mais `SCPS_TUNE=NOM=…` → « tunable INCONNU » + **exit(2)** (prouvé sur binaire), et invisibles au panneau dev F10. | scps_sim.c:170 ; scps_campaign.c:360/395/564 ; scps_econ.c:1313/2841 ; scps_tune.c:48-51/111-112 |
| 7 | **med** | **Le LETTRÉ religieux injouable** : reader `scps_religion_scholar_role` + verbe `recruit_scholar` bindés, mécanique P6 réelle (CONVERT/STABILIZE/RESIST tickée), mais **aucun bouton** — seul le probe d'audit les exerce ; `religion_panel.gd` ne fait que fonder/schismer. | scps_api.h:1000-1001 ; scps_sim_node.cpp:172-173 ; religion_panel.gd:120-202 ; scps_religion.c:171-224 |
| 8 | **med** | **Cliquet lang-check AVEUGLE** : le grep vise des primitives SDL retirées (`draw_text`/`sh_button`…) → 0 match trivial, fausse confiance ; **25 littéraux FR face-joueur** vivent en `return "…"` dans scps_readout.c et atteignent le panneau province (vocation/climat/relief → ScpsProvInfo). | Makefile:656-658 ; scps_readout.c:507-518/584-604/848, :666/667/706 → scps_api.c:396-402 |

Autres med confirmés : **marché servile sans prix affiché** (spread achat ×2 / vente ×1 débité mais jamais montré, membrane muette — sidebar_drawer.gd:422-427, scps_intertrade.c:752 vs :703) ; **E3 stockeuse morte-en-pratique** (0 entrepôt sur seeds 7/9/11/42 × 200 ans malgré AI_HALLES_HUNGER) ; **noms de ministre non injectés** dans les 3 events de trahison (`event_title` n'a qu'un `%s` région/pays — scps_events.c:1827-1838, titres statiques :1515/1534/1554).

### 3 chantiers prioritaires recommandés

1. **Le contrat de save (correctness moteur)** — sérialiser les 3 cooldowns de révolte (nouvelle section, motif EMOB/COLC/TXYR, bump SAVE_VERSION) + clamper `g_hub_of[r] ∈ [-1, n_regions)` au load/`save_sane` + étendre le fuzztest à g_hub_of ; au passage le fix une-ligne du dead-write d'armes (`econ_region_stock_add` + le bon bien macro).
2. **La membrane honnête (UI qui ne ment pas)** — binder `scps_build_legal` (en y ajoutant le gate matière nomat/nocap) et griser/ambrer le bouton Bâtir sur le patron `manuf_legal` ; ajouter RFIN_SANG au readout + faire lire `fin_raw` par le latch d'épilogue ; exposer les prix (marché servile, coût manufacture) ; injecter le nom de ministre dans les events de trahison ; corriger `codex.gd:47` ; câbler le lettré religieux dans religion_panel.
3. **Ré-armer l'outillage** — ré-écrire le grep du cliquet lang-check pour les `return "…"` de scps_readout.c et migrer les 25 littéraux en STR_* ; enregistrer les 6 tunables au registre J (6 lignes X-macro) ; puis re-calibrer les mécaniques inertes mesurées (E3 entrepôts, interception de convoi, péage de détroit) — construites, jamais émergentes sur les graines de référence.

---

## DIMENSION 1 — VERBES → UI

**Verdict global : 38/38 OK.** Lane 1 : chaque `CMD_*` a façade + binding + bouton réel (18 items détaillés, tous OK — construction, recherche, 7 verbes diplo, ordre intérieur, campagne/routes, armée (levée/posture/refill/disband/navy), marché, Conseil (hire/dismiss/**pay avec curseur**), servile (buy/sell/manumit), allocation main-d'œuvre ×4, build_manuf, pop_transfer, colonize, age_engage, event_choice, decree). Le vérificateur (0 item non-OK à re-vérifier) a refait un balayage indépendant complet de la chaîne enum→drain→façade→binding→call-sites GDScript et la confirme **complète et saine** — aucun verbe mort (bindé mais jamais appelé), aucun verbe orphelin sans case de drain.

**Résidu cosmétique** (seul défaut) : `codex.gd:47` — texte périmé sur council_pay (« (bientôt) aucun curseur Godot »), contredit par `sidebar_drawer.gd:294-308` + :822. Chaîne de contenu, pas un défaut de câblage.

## DIMENSION 2 — MÉCANIQUES MORTES

*(Audit indépendant du vérificateur — la lane d'origine est morte en rendant un stub ; 12 items, mesures chronicle sur binaires du 06.)*

### CONFIRMED

| Sév. | Area | Status | Détail | Évidence |
|------|------|--------|--------|----------|
| med | E3 IA stockeuse (BLD_ENTREPOT/TECH_HALLES) | BROKEN | 0 entrepôt sur seeds 7/9/11/42 × 200 ans ; σ prix Centres = 0. Le correctif AI_HALLES_HUNGER (censé produire 6-10/sim) est présent mais TECH_HALLES quasi jamais débloquée / gate credit_trade≥1 rarement atteint. Mécanisme entier mort en pratique. | scps_ai.c:1368, :2137-2192 ; télémétrie chronicle 4 seeds |
| low | TechState.eco / .mil | BROKEN | Champs écrits (init, `tech_research += dEco/dMil`, loquet diffusion) et **sérialisés** en blob, lus par PERSONNE (grep exhaustif = 0 lecteur) — tous les deltas dEco/dMil de l'arbre sont du calcul mort. Contraste : `puissance` et `F` SONT lus (la note CLAUDE.md disant « F mort » était fausse). | scps_tech.c:453/622-623/439 ; scps_save.c:121/380 ; scps_prosperity.c:225 ; scps_core.c:68 |
| low | Interception de convoi | BROKEN | 0 convoi coulé / 0 paquet noyé sur seeds 7/9/42 × 200 ans (500-1050 marins embarqués). La course navale VIT (65 raids/6490 or seed 9) ; le sous-mécanisme « transport sans escorte = proie » ne tire jamais. | télémétrie chronicle « 0 interception(s) » 3 seeds |
| low | Péage de détroit | PARTIAL | Détection (11-22 goulets) et tenue (5-10) vivent, mais la MONÉTISATION est quasi-morte : 0 route taxée/0 or seeds 7/9/11 ; 58 or sur seed 42 seulement. | télémétrie chronicle 4 seeds |
| low | Marché servile (pool Centres) + affranchissement | PARTIAL | 279 âmes serviles existent (seed 9) et l'invariant strate↔groupe est vert (demography_demo 44/44), mais la circulation (vente au pool, gate slaves≥10) et l'affranchissement restent à 0. | télémétrie « 0 au pool · 0 affranchissement(s) » |
| low | Transmuteurs FOREUSE/RÉPLICATEUR | PARTIAL | 0 bâtis sur 4 seeds × 200 ans (seule la Corne vit, 1× seeds 9/11) : les déclencheurs de RECHERCHE (famine fer §4 / bois-grain FAU5) ne tirent quasi jamais → tech jamais débloquée → build jamais atteint. Contribution à l'entropie §27 marginale. | EDI_DBG 4 seeds ; scps_ai.c:1002-1031 |

### REFUTED (faux positifs écartés)

- **Chaîne FEU « ne produit jamais »** — REFUTED : vivante sur seed 42 (100 % servi, 5.2/tick) ; morte seeds 9/11 faute de salpêtre+éthos martial. Conditionnelle au monde, pas morte. (Et le goulot d'armes général « 7.8 % servi » de TROUVAILLES est périmé : mesure fraîche 73-95 % servi.)
- **H_coerc jamais lu au siège** — REFUTED : LOT 3 landé, `region_defense` inclut `DEF_PER_H·H_coerc` (scps_campaign.c:93).
- **Garnison jamais posée par l'IA** — REFUTED : EDI_DBG seed 9 → Garnison/Forteresse/Citadelle made=1 chacun (via le fork de crise, scps_ai.c:1250-1252). Rare mais pas mort.
- **4 verbes servile/council_pay sans UI** — REFUTED : câblés bout-en-bout (cf. dimension 1).
- **campaign_disband mort / warhost_disband perd le fer** — REFUTED : les deux fixes W-GUERRE ont landé (scps_campaign.c:773 ; scps_warhost.c:172→wh_shed).
- **Nudge de prix intertrade dead-write résiduel** — REFUTED : correctement RETIRÉ (commentaires seuls aux ex-sites ; les accès price restants sont des lectures d'arbitrage légitimes).

## DIMENSION 3 — RESTES (recensement des différés / À VENIR / TODO)

*(Lane 3 : 32 items recensés depuis TROUVAILLES.md/CLAUDE.md/eco_fable.md + grep ; 17 re-vérifiés adversarialement : 14 CONFIRMED, 3 REFUTED.)*

### BROKEN (vrais défauts, confirmés)

| Sév. | Area | Détail | Évidence |
|------|------|--------|----------|
| med | endgame — FIN_SANG hors readout | **Aggravé par le vérificateur** : au-delà du bandeau muet, l'épilogue ne s'ouvre JAMAIS sur une fin SANG (latch `fin>0` avec fin=0). Fix : RFIN_SANG + case, OU lire fin_raw dans banner/main. | scps_readout.c:473-478 ; main.gd:343-347 ; epilogue.gd |
| med | éco — E3 stockeuse inerte | Re-mesuré au chiffre près : 0/1/0 entrepôts (seeds 9/7/11) ; SCPS_E3DIAG = 0 échantillon. | chronicle ; scps_ai.c:111/1368/2137 |
| med | events — noms de ministre absents | Les 3 titres de trahison sont des chaînes statiques SANS %s ; `event_title` ne résout qu'un %s région/pays. `statecraft_council_cand_name` existe mais inutilisé ici. | scps_events.c:1827-1838, :1515/1534/1554 |
| low | IA faustienne — foreuse/réplicateur conso 0 | Pipeline complet, gates amont (recherche) jamais atteints — pas une régression. | chronicle 3 seeds ; scps_ai.c:1002-1031 |

### DEFERRED (choix documentés / assumés — pas des bugs)

- **9 #define SOLDE_*/WH_*** hors registre J (scps_warhost.c:35-42, scps_army.c:137-140, marqueurs « À MIGRER » présents ; miroirs hardcodés chronicle.c:539-542 ; registre libre). Dette de refactoring pure. *(Note vérif : SOLDE_FL_PER_REG vaut 0.7 — l'entrée TROUVAILLES qui disait 0.5 est périmée.)*
- **Garnison hors doctrine militaire** : `ai_next_h_edifice` appelé UNIQUEMENT via le fork de crise (gate brake+w_expand≥0.60), jamais par la branche doctrine (scps_ai.c:1250-1255 vs :1269-1271). Design gap réel, cohérent avec la note CLAUDE.md — et l'effet mécanique (DEF_PER_H) existe si bâtie.
- **Chantier-au-fil-de-l'eau** (Anno) : la matière est consommée en UNE passe au gate (scps_agency.c:339-368) ; explicitement optionnel depuis le recalage des coûts.
- **Worldgen #3 — ré-accumulation hydrologique post-lacs** : les embouchures excluent les cellules lac, pas de recompute flux D8 après le flood — hors scope assumé, greenlight requis ; non observable (22 lacs/64 rivières sains).
- **Reorg BASE_COST tiers tech** : inchangé volontairement (re-baseline pour gain cosmétique) ; lisibilité par anneaux de tier (tech_panel.gd:257/308).
- **Lavis endgame jamais exercé sur une fin réelle** : gate an-180 → aucun probe court ne l'atteint ; structure prouvée correcte (fin_raw + clé 5). Testable via SCPS_TUNE.
- **SUCCESSION = ton, pas acte** : bloc de mutation vide DÉLIBÉRÉ (commentaire scps_events.c:2190-2198) — `council_age_tick` vide le siège de toute façon.
- **Rot/loyauté du Conseil jamais observé vibrer** : loyauté 93/89 sur 100, 0.0 au bord, 0.0 remplacement (seeds 9/7 × 200 ans) — un monde sain ne stresse pas ses conseillers ; prouvé isolément en banc.
- **Nourriture ∝ fertilité (#5 suite)** : largement SUPERSÉDÉ par la refonte éco #2 (out ∝ geo_eff = raw_cap/GEO_REF) + RES_FRUIT ; item de roadmap probablement obsolète.
- **T8 design** : reste ouvert (une des 2 pistes — compacité bas-régime — déjà implémentée, l'autre — plancher d'agression N<4 — non).
- **Parchemin, genuinement absents** : PORTS visibles (reader façade `scps_region_has_port` à créer + rebuild), lignes de commerce, hachures d'AGITATION/révolte (distinctes du siège), survol lumineux de province.

### Résolus silencieusement (couverture — la lane 3 les a vérifiés clos)

Nudge intertrade retiré (plus « commenté ») · CMD_DISBAND rend les armes (LOT 2) · compteur « régions réduites » exact (LOT 6a) · H_coerc lu au siège (LOT 3) · army_demo couvre les 13 unités (LOT 6b) · gates techno des unités (LOT 5) · hachures de siège + panneau de combat (LOTS A/B) · verbes serviles UI (V3) · lavis apocalypse par variante (LOT 1 V3) · events de trahison écrits (V2b) · 6 lots UI Province · TODO n_groups = fallback défensif vivant, pas un bug · C7 endgame fait côté Godot (C8 pondération IA reste différé assumé) · PMOD_ADMIN (pendant démo du boost K).

### REFUTED (faux positifs de la lane 3)

- **« CMD_COUNCIL_PAY sans curseur UI »** — FAUX : curseur 0.5×/1×/1.5×/2× par siège entièrement câblé (sidebar_drawer.gd:294-308 → player_council_pay). La trouvaille s'appuyait sur des entrées TROUVAILLES périmées. Seul vestige : le label « (bientôt) » de codex.gd:47.
- **« T8 : aucun match dans le code »** — FAUX : T8 est spécifié en détail (sweep/RAPPORT_SWEEP_2026-07-03.md:89-95) ET a du code d'adressage (scps_world.c:3944-3957 + WORLD_EMP_COMFORT, scps_tune_list.h:224). Le statut « ouvert » reste défendable, l'évidence était fausse.
- **« Parchemin priorités 3-4 toutes non faites »** — PARTIELLEMENT PÉRIMÉ : EAU statut-carte (shader :209-291), hachures de SIÈGE et liseré de capitale sont FAITS depuis. Restent : ports, lignes de commerce, hachures d'agitation, hover-glow.

## DIMENSION 4 — MEMBRANE, FAÇADE & TUNABLES

*(8 items, TOUS CONFIRMED par le vérificateur — aucun faux positif ; preuve empirique sur binaire pour le rejet SCPS_TUNE.)*

| Sév. | Area | Status | Détail | Évidence |
|------|------|--------|--------|----------|
| high | Bouton Construction NO-OP | BROKEN | Grise par TECH seule ; refus silencieux au drain (or/matière) ; flash « ordre émis » mensonger ; `scps_build_legal` mort (non bindé, 0 call). Nuance vérif : build_legal ne teste que région+doublon+or, PAS la matière — à compléter en le câblant. Contre-exemple sain : `manuf_legal` (même mécanique, bindé, grise l'insolvabilité). | construction_panel.gd:86/167-169 ; scps_sim.c:336 ; scps_agency.c:336/345 ; scps_api.c:1221-1229 |
| med | 6 tune_f hors registre | PARTIAL | `SCPS_TUNE=BT_ATK_RATIO=2.0` → « tunable INCONNU » + exit(2) MESURÉ ; invisibles à `--tunables`/F10. Fix : 6 lignes X(NOM,def). | scps_tune.c:48-51/70-73/111-112 ; sites exacts 6 fichiers |
| med | Lettré religieux sans UI | MISSING | Reader+verbe bindés, mécanique P6 réelle, 0 bouton (seul le probe religion_audit.gd:83). | scps_api.h:1000-1001 ; religion_panel.gd ; scps_religion.c:171-224 |
| med | Marché servile — prix caché | PARTIAL | Pool en count seul ; spread ×2/×1 débité au drain jamais montré (le reader n'expose pas de prix) ; commentaire « prix courant » non tenu. | sidebar_drawer.gd:395/422-453 ; scps_intertrade.c:752/703 |
| med | Cliquet lang-check aveugle | BROKEN | Le grep matche 0 (primitives SDL retirées), base 0 → passe trivialement ; 25 littéraux FR de scps_readout.c atteignent le panneau province. « BROKEN » = garde aveugle (le script tourne, exit 0). | Makefile:656-658 ; scps_readout.c (25 `return "…"`) → scps_api.c:396-402 |
| low | Coût manufacture non affiché | PARTIAL | Display et drain COHÉRENTS (même formule MANUF_BUILD_COST×ipm, manuf_legal grise l'insolvabilité) — il manque juste l'exposition du montant. Pas un no-op. | province_detail.gd:402-424 ; scps_sim.c:429-433 ; scps_api.c:1660-1661 |
| low | 6 bindings morts/redondants | PARTIAL | 0 call .gd : country_gold (redondant avec country_info().or) · map_image/scps_map_rgba (carte rendue par layer/variant/political_image) · border_segments plain (supplanté par _col) · region_settle_group (orphelin parchemin) · region_sunken (eau via variant_map_image) · world_pop (jamais affiché). Nettoyage ou câblage. *(Imprécision relevée : map_settlements=scps_api.h:189 est un commentaire, pas un binding.)* | grep exact-mot frais ; scps_sim_node.cpp:329 ; alerts.gd:187 |
| low | Codex périmé (council_pay) | PARTIAL | « (bientôt) aucun curseur » contredit par le curseur câblé et fonctionnel. | codex.gd:47 vs sidebar_drawer.gd:294-308/:822 |

## DIMENSION 5 — CORRECTNESS, SAVE & DÉTERMINISME

*(4 classes examinées ; 3 trouvailles, TOUTES CONFIRMED par le vérificateur.)*

| Sév. | Area | Status | Détail | Évidence |
|------|------|--------|--------|----------|
| high | Cooldowns de révolte non sérialisés | BROKEN | `g_revolt_grace`/`g_coup_grace`/`g_concede_cd` : gates de décision moteur posés par revolt_ignite, décrémentés chaque jour, RAZ seulement par revolt_init (sim_init) — JAMAIS restaurés au load (RVLT ne sérialise que RevoltState). Save prise en fenêtre de grâce + reload processus frais → révolte/coup/concession immédiats possibles → **save/reload ≠ continuation**. Même jurisprudence EMOB/COLC/TXYR (TROUVAILLES:58-60). Non capté par --savetest (same-process, les statics survivent). Fix : sérialiser les trois (nouvelle section) + borner au load. | scps_revolt.c:118/124/206/:290/:344/:913/:406/:867/:930/:209-211 ; scps_save.c:129/:388 ; scps_sim.c:1020 ; viewer.c:206-213 |
| med | Dead-write armes IA (Dominateur/Honneur) | BROKEN | `cre->stock[RES_ARMS]+=20` sur la vue region[] (écrasée par aggregate memset+rebuild+copyback) ; PB_AI avant PB_ECON ; l'or est réellement débité. **Bonus vérif** : la levée consomme les macros RES_ARMS_LIGHT/HEAVY/RANGED/FIREARM, pas la base RES_ARMS → doublement mort ; le fix une-ligne (econ_region_stock_add) doit AUSSI corriger le mismatch base↔macro. | scps_ai.c:1303/1307 ; scps_econ.c:890/923/993/1813 ; scps_credit.c:73 ; scps_army.c:109-114 ; scps_warhost.c:392 |
| med | save_sane ne borne pas ITRD (g_hub_of) | PARTIAL | int16_t non clampé au load ; `e->region[hub].owner/.stock` déréférencé avec les seuls gardes hub≥0 && hub≠region (aucune borne haute) → OOB READ sur save forgée (le modèle de menace explicite de save_sane). **Aggravant vérif** : `intertrade_buy_cost` (:436) n'a AUCUN rebuild dirty et est atteint au 1er tick de bâti post-load (agency_build_gold) → l'OOB tombe même g_hub_dirty=true. Fuzztest ne forge jamais g_hub_of. Fix : clamp ∈[-1,n_regions) dans intertrade_load ou save_sane. | scps_intertrade.c:816-831/:436/:530 ; scps_save.c:182-345/:361 ; scps_agency.c:301 ; viewer.c:243-251 |
| low | Déterminisme (RNG/horloge) | OK | RAS — aucun rand()/horloge-mur dans un chemin moteur ; le nonce de save (time^clock) chiffre le payload sans entrer dans l'état déterministe ; religion_found_random seedé par hash d'état ; time(NULL) seulement dans les outils (sélection de graine). | grep exhaustif ; scps_save.c:171-173 ; scps_ai.c:1471 |

Les écritures directes region[] hors helpers restantes sont toutes dans les *_demo.c (fixtures légitimes) ou déjà re-keyées vers prov[].

---

## NOTE DE FRAÎCHEUR (travail atterri le 2026-07-07, après le snapshot d'audit)

1. **« moment_treason non câblé » → RÉSOLU-PAR-SUPPRESSION** : moment_treason.wav a été supprimé le 07 (purge des sons procéduraux, commit 090d4dd — passage au modèle « un clic = un son », façon Paradox). Ne PAS le câbler.
2. Tout item qui recommanderait des **ambiances/drones** est caduc : le système d'ambiance a été retiré délibérément.
3. La **re-découpe des planches parchemin** (centrage) et l'intégration **écrans de fin / bordures enluminées** sont FAITES le 07 (commits 58514fd, a400f80) — les restes correspondants sont RÉSOLUS. (Attention : la fin SANG reste, elle, invisible au readout — défaut #3 ci-dessus, indépendant des assets.)
